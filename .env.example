# Optional: restrict cross-origin HTTP API access to callers that supply this key.
# If unset, the API is open to all origins (suitable for local use).
LOGOKIT_API_KEY=

# Optional: comma-separated list of origins allowed to call the API without a key.
# Example: https://myapp.com,https://staging.myapp.com
LOGOKIT_ALLOWED_ORIGINS=
